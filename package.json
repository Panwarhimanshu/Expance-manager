{
  "name": "expance-manager",
  "private": true,
  "version": "1.0.0",
  "description": "Stall Ledger - Byte Club expense/sales tracker (React + Node/Express + MongoDB)",
  "scripts": {
    "server": "node server/index.js",
    "server:dev": "node --watch server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.6.3"
  }
}
